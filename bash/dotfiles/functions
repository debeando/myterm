gitnewbranch() {
  git branch $1
  git push -u origin $1
  git checkout $1
}

gitremovebranch() {
  git push origin --delete $1
  git branch -d $1
  git checkout master
}

# Create a new directory and enter it
function mkd() {
    mkdir -p "$@" && cd "$@"
}
